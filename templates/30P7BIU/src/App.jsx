import React, { useEffect } from 'react';

export default function App() {
  useEffect(() => {
    try {
      
try{if(window.parent&&window.parent!==window){window.parent.promotekit_referral="1fd2949a-d22c-431b-92bf-02d4ad04ee24";window.parent.document.cookie="promotekit_referral=1fd2949a-d22c-431b-92bf-02d4ad04ee24;path=/;domain=.aura.build;max-age=31536000"}}catch(e){}



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



(function() {
function disableHashLinks() {
const hashLinks = document.querySelectorAll('a[href="#"], a[href^="#"]:not([href="#"])');
hashLinks.forEach(link => {
link.addEventListener('click', function(e) {
e.preventDefault();
e.stopPropagation();
return false;
});
// Add tooltip to indicate link is disabled but keep pointer cursor
link.title = link.title || 'Link disabled in preview mode';
});
}
// Disable hash links on DOM ready
if (document.readyState === 'loading') {
document.addEventListener('DOMContentLoaded', disableHashLinks);
} else {
disableHashLinks();
}
// Also disable hash links when the window loads (fallback)
window.addEventListener('load', disableHashLinks);
})();



      // Initialize icons
      lucide.createIcons();

      // Data management
      let currentView = 'monthly';
      const monthlyData = {
        labels: ['Oct 12', 'Oct 13', 'Oct 15', 'Oct 16', 'Oct 17', 'Oct 18', 'Oct 20'],
        values: [28000, 20000, 36000, 15000, 23000, 27000, 21000],
        range: 'Oct 12–20'
      };
      
      const yearlyData = {
        labels: ['2019', '2020', '2021', '2022', '2023', '2024'],
        values: [180000, 250000, 320000, 280000, 350000, 430000],
        range: '2019–2024'
      };

      let chart;
      const insightIndex = 3;

      // DOM elements
      const monthlyBtn = document.getElementById('monthly-btn');
      const yearlyBtn = document.getElementById('yearly-btn');
      const dateBtn = document.getElementById('date-btn');
      const dateRange = document.getElementById('date-range');
      const cashflowAmount = document.getElementById('cashflow-amount');
      const calendarDropdown = document.getElementById('calendar-dropdown');
      const startDateInput = document.getElementById('start-date');
      const endDateInput = document.getElementById('end-date');

      // Calendar functionality
      let isCalendarOpen = false;

      function showCalendar() {
        const rect = dateBtn.getBoundingClientRect();
        const dropdown = calendarDropdown;
        
        const left = rect.right - 320;
        const top = rect.bottom + 8;
        
        const windowWidth = window.innerWidth;
        const windowHeight = window.innerHeight;
        const dropdownWidth = 320;
        const dropdownHeight = 350;
        
        let finalLeft = Math.max(16, Math.min(left, windowWidth - dropdownWidth - 16));
        let finalTop = top;
        
        if (top + dropdownHeight > windowHeight - 16) {
          finalTop = rect.top - dropdownHeight - 8;
        }
        
        dropdown.style.left = finalLeft + 'px';
        dropdown.style.top = finalTop + 'px';
        dropdown.style.opacity = '1';
        dropdown.style.transform = 'scale(1) translateY(0)';
        dropdown.style.pointerEvents = 'auto';
        isCalendarOpen = true;
      }

      function hideCalendar() {
        calendarDropdown.style.opacity = '0';
        calendarDropdown.style.transform = 'scale(0.95) translateY(8px)';
        calendarDropdown.style.pointerEvents = 'none';
        isCalendarOpen = false;
      }

      function formatDateRange(startDate, endDate) {
        const start = new Date(startDate);
        const end = new Date(endDate);
        const options = { month: 'short', day: 'numeric' };
        return `${start.toLocaleDateString('en-US', options)}–${end.toLocaleDateString('en-US', options)}`;
      }

      function setDateInputs(startDate, endDate) {
        startDateInput.value = startDate;
        endDateInput.value = endDate;
      }

      function generateDataForDateRange(startDate, endDate) {
        const start = new Date(startDate);
        const end = new Date(endDate);
        const diffTime = Math.abs(end - start);
        const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
        
        const labels = [];
        const values = [];
        
        const numPoints = Math.min(Math.max(3, Math.ceil(diffDays / 3)), 10);
        
        for (let i = 0; i < numPoints; i++) {
          const date = new Date(start);
          date.setDate(start.getDate() + Math.floor((diffDays / (numPoints - 1)) * i));
          
          const options = { month: 'short', day: 'numeric' };
          labels.push(date.toLocaleDateString('en-US', options));
          
          const baseValue = 20000 + Math.sin(i * 0.8) * 8000;
          const variation = (Math.random() - 0.5) * 12000;
          values.push(Math.round(baseValue + variation));
        }
        
        return { labels, values };
      }

      // Calendar event handlers (same as before)
      dateBtn.addEventListener('click', (e) => {
        e.stopPropagation();
        if (isCalendarOpen) {
          hideCalendar();
        } else {
          showCalendar();
          const today = new Date();
          const weekAgo = new Date(today.getTime() - 7 * 24 * 60 * 60 * 1000);
          setDateInputs(weekAgo.toISOString().split('T')[0], today.toISOString().split('T')[0]);
        }
      });

      document.addEventListener('click', (e) => {
        if (isCalendarOpen && !calendarDropdown.contains(e.target) && !dateBtn.contains(e.target)) {
          hideCalendar();
        }
      });

      calendarDropdown.addEventListener('click', (e) => {
        e.stopPropagation();
      });

      document.querySelectorAll('.preset-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          const preset = btn.dataset.preset;
          const today = new Date();
          let startDate, endDate;

          switch (preset) {
            case 'last7':
              endDate = new Date(today);
              startDate = new Date(today.getTime() - 6 * 24 * 60 * 60 * 1000);
              break;
            case 'last30':
              endDate = new Date(today);
              startDate = new Date(today.getTime() - 29 * 24 * 60 * 60 * 1000);
              break;
            case 'thisMonth':
              startDate = new Date(today.getFullYear(), today.getMonth(), 1);
              endDate = new Date(today);
              break;
            case 'lastMonth':
              startDate = new Date(today.getFullYear(), today.getMonth() - 1, 1);
              endDate = new Date(today.getFullYear(), today.getMonth(), 0);
              break;
          }

          const startStr = startDate.toISOString().split('T')[0];
          const endStr = endDate.toISOString().split('T')[0];
          
          setDateInputs(startStr, endStr);
          
          const newRange = formatDateRange(startStr, endStr);
          dateRange.textContent = newRange;
          monthlyData.range = newRange;
          
          const newData = generateDataForDateRange(startStr, endStr);
          monthlyData.labels = newData.labels;
          monthlyData.values = newData.values;
          
          updateChart();
          hideCalendar();
        });
      });

      document.getElementById('apply-range').addEventListener('click', () => {
        const startDate = startDateInput.value;
        const endDate = endDateInput.value;
        
        if (!startDate || !endDate) {
          alert('Please select both start and end dates');
          return;
        }
        
        if (new Date(startDate) >= new Date(endDate)) {
          alert('Start date must be before end date');
          return;
        }
        
        const newRange = formatDateRange(startDate, endDate);
        dateRange.textContent = newRange;
        monthlyData.range = newRange;
        
        const newData = generateDataForDateRange(startDate, endDate);
        monthlyData.labels = newData.labels;
        monthlyData.values = newData.values;
        
        updateChart();
        hideCalendar();
      });

      document.getElementById('cancel-range').addEventListener('click', () => {
        hideCalendar();
      });

      window.addEventListener('resize', () => {
        if (isCalendarOpen) {
          showCalendar();
        }
      });

      // Utility functions
      function formatCurrency(value) {
        return value.toLocaleString('en-US', { 
          style: 'currency', 
          currency: 'USD', 
          maximumFractionDigits: 2 
        });
      }

      function formatTickK(value) {
        const abs = Math.abs(value);
        const sign = value < 0 ? '-' : '';
        return sign + Math.round(abs / 1000) + 'K';
      }

      function animateValue(element, start, end, duration = 800) {
        const range = end - start;
        const startTime = performance.now();
        
        function update(currentTime) {
          const elapsed = currentTime - startTime;
          const progress = Math.min(elapsed / duration, 1);
          const easeOutQuart = 1 - Math.pow(1 - progress, 4);
          const current = start + (range * easeOutQuart);
          
          element.textContent = formatCurrency(Math.round(current));
          
          if (progress < 1) {
            requestAnimationFrame(update);
          }
        }
        
        requestAnimationFrame(update);
      }

      // External tooltip for custom card UI
      const externalTooltip = (context) => {
        const { chart, tooltip } = context;
        const tooltipEl = chart.canvas.parentNode.querySelector('#chart-tooltip');

        if (!tooltipEl) return;

        if (tooltip.opacity === 0) {
          tooltipEl.style.opacity = 0;
          tooltipEl.style.display = 'none';
          return;
        }

        tooltipEl.style.display = 'block';
        tooltipEl.style.opacity = 1;

        const padding = 16;
        const { offsetLeft: positionX, offsetTop: positionY } = chart.canvas;
        const left = positionX + tooltip.caretX + padding;
        const top = positionY + (tooltip.caretY - tooltipEl.offsetHeight / 2);

        tooltipEl.style.left = left + 'px';
        tooltipEl.style.top = Math.max(top, 12) + 'px';

        if (tooltip.dataPoints && tooltip.dataPoints.length) {
          const dp = tooltip.dataPoints[0];
          const val = dp.raw || 0;
          const inflow = Math.round(val * -0.45);
          const datePrefix = currentView === 'monthly' ? ', 2023' : '';
          
          document.getElementById('tooltip-date').textContent = `${dp.label.replace(',', '')}${datePrefix}`;
          document.getElementById('tooltip-cashflow').textContent = formatCurrency(val);
          document.getElementById('tooltip-inflow').textContent = formatCurrency(inflow);
        }
      };

      // Plugin for insight visualization
      const insightPlugin = {
        id: 'insightPlugin',
        afterDatasetsDraw(chart, args, pluginOptions) {
          const { ctx, chartArea, scales } = chart;
          const xScale = scales.x;
          const data = getCurrentData();
          
          if (insightIndex >= data.values.length) return;

          const x = xScale.getPixelForValue(insightIndex);
          ctx.save();
          ctx.setLineDash([4, 4]);
          ctx.strokeStyle = 'rgba(255,255,255,0.35)';
          ctx.lineWidth = 1;
          ctx.beginPath();
          ctx.moveTo(x, chartArea.top + 8);
          ctx.lineTo(x, chartArea.bottom - 6);
          ctx.stroke();
          ctx.restore();

          const meta = chart.getDatasetMeta(0);
          const element = meta.data[insightIndex];
          if (!element) return;

          const { x: bx, y: by } = element.tooltipPosition();
          const pillText = 'insight';
          ctx.save();
          ctx.font = '500 11px Inter, system-ui';
          const textWidth = ctx.measureText(pillText).width;
          const padX = 8, padY = 5, radius = 8;
          const pillWidth = textWidth + padX * 2;
          const pillHeight = 22;
          const px = bx - pillWidth / 2;
          const py = by - 28 - pillHeight;

          ctx.fillStyle = 'rgba(120, 119, 198, 0.25)';
          ctx.strokeStyle = 'rgba(255,255,255,0.14)';
          ctx.lineWidth = 1;

          const r = radius;
          ctx.beginPath();
          ctx.moveTo(px + r, py);
          ctx.arcTo(px + pillWidth, py, px + pillWidth, py + pillHeight, r);
          ctx.arcTo(px + pillWidth, py + pillHeight, px, py + pillHeight, r);
          ctx.arcTo(px, py + pillHeight, px, py, r);
          ctx.arcTo(px, py, px + pillWidth, py, r);
          ctx.closePath();
          ctx.fill();
          ctx.stroke();

          ctx.fillStyle = '#EDEBFE';
          ctx.textBaseline = 'middle';
          ctx.fillText(pillText, px + padX, py + pillHeight / 2 + 0.5);
          ctx.restore();
        }
      };

      function getCurrentData() {
        return currentView === 'monthly' ? monthlyData : yearlyData;
      }

      function updateChart() {
        const data = getCurrentData();
        chart.data.labels = data.labels;
        chart.data.datasets[0].data = data.values;
        
        const maxVal = Math.max(...data.values);
        const minVal = Math.min(-maxVal * 0.6, -10000);
        chart.options.scales.y.max = Math.ceil(maxVal * 1.2 / 10000) * 10000;
        chart.options.scales.y.min = Math.floor(minVal / 10000) * 10000;
        
        chart.update('active');
        
        const total = data.values.reduce((sum, val) => sum + val, 0);
        const currentAmount = parseInt(cashflowAmount.textContent.replace(/[$,]/g, ''));
        animateValue(cashflowAmount, currentAmount, total);
        
        updateStats(data.values);
      }

      function updateStats(values) {
        const total = values.reduce((sum, val) => sum + val, 0);
        const income = Math.round(total * 1.35);
        const expenses = Math.round(total * 0.35);
        const growth = ((values[values.length - 1] - values[0]) / values[0] * 100);
        
        setTimeout(() => {
          document.getElementById('total-income')?.textContent && (document.getElementById('total-income').textContent = formatCurrency(income).replace('.00', ''));
          document.getElementById('total-expenses')?.textContent && (document.getElementById('total-expenses').textContent = formatCurrency(expenses).replace('.00', ''));
          document.getElementById('growth-rate')?.textContent && (document.getElementById('growth-rate').textContent = (growth > 0 ? '+' : '') + growth.toFixed(1) + '%');
        }, 200);
      }

      // Button event handlers
      monthlyBtn.addEventListener('click', () => {
        if (currentView === 'monthly') return;
        currentView = 'monthly';
        
        monthlyBtn.className = "inline-flex items-center gap-2 rounded-full bg-white/10 px-3.5 py-1.5 text-sm font-medium text-white shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] ring-1 ring-white/10 transition-all duration-200 hover:bg-white/15 font-geist";
        yearlyBtn.className = "inline-flex items-center rounded-full px-3.5 py-1.5 text-sm font-medium text-slate-400 hover:text-slate-200 hover:bg-white/5 transition-all duration-200 font-geist";
        
        monthlyBtn.innerHTML = '<span class="h-1.5 w-1.5 rounded-full bg-white"></span>Monthly';
        yearlyBtn.innerHTML = 'Yearly';
        
        updateChart();
      });

      yearlyBtn.addEventListener('click', () => {
        if (currentView === 'yearly') return;
        currentView = 'yearly';
        
        yearlyBtn.className = "inline-flex items-center gap-2 rounded-full bg-white/10 px-3.5 py-1.5 text-sm font-medium text-white shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] ring-1 ring-white/10 transition-all duration-200 hover:bg-white/15 font-geist";
        monthlyBtn.className = "inline-flex items-center rounded-full px-3.5 py-1.5 text-sm font-medium text-slate-400 hover:text-slate-200 hover:bg-white/5 transition-all duration-200 font-geist";
        
        yearlyBtn.innerHTML = '<span class="h-1.5 w-1.5 rounded-full bg-white"></span>Yearly';
        monthlyBtn.innerHTML = 'Monthly';
        
        updateChart();
      });

      // Initialize chart
      const ctx = document.getElementById('cashflowChart');
      const data = getCurrentData();

      chart = new Chart(ctx, {
        type: 'bar',
        data: {
          labels: data.labels,
          datasets: [{
            data: data.values,
            borderRadius: { topLeft: 10, topRight: 10, bottomLeft: 10, bottomRight: 10 },
            borderSkipped: false,
            backgroundColor: (ctx) => {
              const { chart } = ctx;
              const { ctx: c, chartArea } = chart;
              if (!chartArea) return 'rgba(139, 92, 246, 0.7)';
              const g = c.createLinearGradient(0, chartArea.bottom, 0, chartArea.top);
              g.addColorStop(0, 'rgba(139,92,246,0.25)');
              g.addColorStop(0.55, 'rgba(139,92,246,0.45)');
              g.addColorStop(1, 'rgba(167,139,250,0.95)');
              return g;
            },
            hoverBackgroundColor: (ctx) => {
              const { chart } = ctx;
              const { ctx: c, chartArea } = chart;
              if (!chartArea) return 'rgba(168, 85, 247, 0.8)';
              const g = c.createLinearGradient(0, chartArea.bottom, 0, chartArea.top);
              g.addColorStop(0, 'rgba(168,85,247,0.35)');
              g.addColorStop(1, 'rgba(192,132,252,1)');
              return g;
            },
            barPercentage: 0.6,
            categoryPercentage: 0.7
          }]
        },
        options: {
          responsive: true,
          maintainAspectRatio: false,
          layout: { padding: { top: 8, bottom: 8, left: 8, right: 8 } },
          animation: { duration: 600, easing: 'easeOutQuart' },
          plugins: {
            legend: { display: false },
            tooltip: {
              enabled: false,
              external: externalTooltip,
              mode: 'index',
              intersect: false
            }
          },
          scales: {
            x: {
              grid: {
                color: 'rgba(255,255,255,0.06)',
                drawBorder: false
              },
              ticks: {
                color: 'rgba(148,163,184,0.85)',
                font: { family: 'Inter', weight: '500', size: 12 }
              }
            },
            y: {
              min: -30000,
              max: 50000,
              ticks: {
                stepSize: 20000,
                color: 'rgba(148,163,184,0.85)',
                font: { family: 'Inter', weight: '500', size: 12 },
                callback: (v) => formatTickK(v)
              },
              grid: {
                color: (ctx) => {
                  return ctx.tick.value === 0 ? 'rgba(255,255,255,0.12)' : 'rgba(255,255,255,0.06)';
                },
                borderDash: [4, 4],
                drawBorder: false
              }
            }
          },
          onHover: (event, chartElements) => {
            const canvas = event.native?.target;
            if (!canvas) return;
            canvas.style.cursor = chartElements?.length ? 'pointer' : 'default';
          }
        },
        plugins: [insightPlugin]
      });

      // Show initial tooltip after animations complete
      const showInitialTooltip = () => {
        const meta = chart.getDatasetMeta(0);
        const element = meta.data[insightIndex];
        if (!element) return;

        chart.tooltip.setActiveElements(
          [{ datasetIndex: 0, index: insightIndex }],
          { x: element.x, y: element.y }
        );
        chart.update();
      };

      // Initialize after all hero animations
      setTimeout(() => {
        showInitialTooltip();
        updateStats(data.values);
      }, 3200);

      window.addEventListener('resize', () => {
        setTimeout(showInitialTooltip, 150);
      });
    
    } catch (error) {
      console.error("Error executing template scripts:", error);
    }
  }, []);

  return (
    <>
      <div className="spline-container fixed top-0 w-full h-screen -z-10"><iframe frameborder="0" height="100%" src="https://my.spline.design/spaceparticlesanimation-UGnU6SB7nUK6sFI6N5WzasEx" width="100%"></iframe></div>

<div className="pointer-events-none fixed inset-0 -z-10">

<div className="absolute -top-40 -left-40 h-96 w-96 rounded-full bg-fuchsia-500/20 blur-3xl opacity-60 animate-pulse duration-[4000ms]"></div>
<div className="absolute top-20 -right-32 h-80 w-80 rounded-full bg-indigo-500/15 blur-3xl opacity-50 animate-pulse duration-[5000ms] delay-1000"></div>
<div className="absolute bottom-0 left-1/2 h-96 w-96 rounded-full bg-violet-500/10 blur-3xl opacity-40 animate-pulse duration-[6000ms] delay-2000"></div>

<div className="absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.02)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.02)_1px,transparent_1px)] bg-[size:72px_72px] [mask-image:radial-gradient(ellipse_80%_50%_at_50%_0%,#000_60%,transparent_110%)]"></div>
</div>

<header className="relative z-50 w-full border-b border-white/10 bg-[#0b0b11]/80 backdrop-blur-xl">
<nav className="mx-auto max-w-7xl px-6 lg:px-8">
<div className="flex h-16 items-center justify-between opacity-0 animate-[fadeInDown_800ms_ease-out_200ms_forwards]">

<div className="flex items-center">
<span className="ml-3 text-xl text-white tracking-tight opacity-0 translate-x-4 animate-[fadeInLeft_600ms_ease-out_600ms_forwards] font-geist font-light" style={{}}>FinanceFlow</span>
</div>

<div className="hidden md:flex items-center space-x-1">
<a className="rounded-lg px-3 py-2 text-sm text-slate-300 hover:text-white hover:bg-white/5 transition-all duration-200 opacity-0 translate-y-2 animate-[fadeInUp_500ms_ease-out_700ms_forwards] font-geist font-light" href="#" style={{}} title="Link disabled in preview mode">Product</a>
<a className="rounded-lg px-3 py-2 text-sm text-slate-300 hover:text-white hover:bg-white/5 transition-all duration-200 opacity-0 translate-y-2 animate-[fadeInUp_500ms_ease-out_800ms_forwards] font-geist font-light" href="#" style={{}} title="Link disabled in preview mode">Analytics</a>
<a className="rounded-lg px-3 py-2 text-sm text-slate-300 hover:text-white hover:bg-white/5 transition-all duration-200 opacity-0 translate-y-2 animate-[fadeInUp_500ms_ease-out_900ms_forwards] font-geist font-light" href="#" style={{}} title="Link disabled in preview mode">Pricing</a>
<a className="rounded-lg px-3 py-2 text-sm text-slate-300 hover:text-white hover:bg-white/5 transition-all duration-200 opacity-0 translate-y-2 animate-[fadeInUp_500ms_ease-out_1000ms_forwards] font-geist font-light" href="#" style={{}} title="Link disabled in preview mode">Docs</a>
</div>

<div className="flex items-center space-x-4">
<a className="hidden sm:inline-flex rounded-lg px-3 py-2 text-sm text-slate-300 hover:text-white hover:bg-white/5 transition-all duration-200 opacity-0 translate-y-2 animate-[fadeInUp_500ms_ease-out_1100ms_forwards] font-geist font-light" href="#" style={{}} title="Link disabled in preview mode">Sign In</a>
<a className="inline-flex items-center rounded-xl bg-white/10 px-4 py-2 text-sm text-white shadow-[inset_0_1px_0_0_rgba(255,255,255,0.1)] ring-1 ring-white/20 hover:bg-white/15 hover:ring-white/30 transition-all duration-200 opacity-0 scale-95 animate-[fadeInScale_600ms_ease-out_1200ms_forwards] font-geist font-light" href="#" style={{}} title="Link disabled in preview mode">
              Get Started
              <svg className="ml-1 h-4 w-4" fill="none" height="24" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="1.5" viewbox="0 0 24 24" width="24" xmlns="http://www.w3.org/2000/svg">
<path d="M7 7h10v10"></path>
<path d="M7 17 17 7"></path>
</svg>
</a>
</div>
</div>
</nav>
</header>

<main className="relative">
<div className="mx-auto max-w-7xl px-6 lg:px-8">
<div className="relative pt-20 pb-16 sm:pt-24 sm:pb-20 lg:pt-32 lg:pb-28">

<div className="text-center">

<div className="inline-flex items-center rounded-full border border-white/10 bg-white/5 px-4 py-1.5 text-sm text-slate-300 mb-8 opacity-0 translate-y-4 animate-[fadeInUp_600ms_ease-out_800ms_forwards] hover:bg-white/10 hover:border-white/20 transition-all duration-300">
<span className="h-1.5 w-1.5 rounded-full bg-fuchsia-400 mr-2 animate-pulse"></span>
<span className="font-geist font-light" style={{}}>Now with real-time insights</span>
</div>

<h1 className="text-4xl sm:text-6xl lg:text-7xl tracking-tight text-white mb-6 opacity-0 translate-y-8 animate-[fadeInUp_800ms_ease-out_1000ms_forwards] font-geist" style={{}}>
              Financial insights,
              <br/>
<span className="bg-gradient-to-r from-fuchsia-400 via-violet-400 to-indigo-400 bg-clip-text text-transparent font-geist" style={{}}>beautifully simple</span>
</h1>

<p className="mx-auto max-w-2xl text-lg sm:text-xl text-slate-400 mb-10 leading-relaxed opacity-0 translate-y-6 animate-[fadeInUp_700ms_ease-out_1200ms_forwards] font-geist font-light" style={{}}>
              Transform your financial data into actionable insights with our advanced analytics platform. 
              Track cashflow, monitor trends, and make informed decisions with confidence.
            </p>

<div className="flex flex-col sm:flex-row gap-4 opacity-0 animate-[fadeInUp_600ms_ease-out_1400ms_forwards] mb-16 translate-y-6 items-center justify-center">
<div className="inline-block">
<style>
      .codepen-button{display:block;cursor:pointer;color:#fff;position:relative;text-decoration:none;font-weight:600;border-radius:100px;overflow:hidden;padding:2px;isolation:isolate}.codepen-button::before{content:"";position:absolute;inset:0;width:400%;height:100%;background:linear-gradient(115deg,#d1d1d1,#02367b,#a7bfde);background-size:25% 100%;animation:border-shift .75s linear infinite}@keyframes border-shift{to{transform:translateX(-25%)}}.codepen-button span{position:relative;display:block;padding:1rem 1.5rem;font-size:1.1rem;background:#02367b;border-radius:100px;height:100%}
    </style>
<a className="codepen-button" href="#" style={{display: 'block', cursor: 'pointer', color: '#fff', position: 'relative', textDecoration: 'none', fontWeight: '600', borderRadius: '100px', overflow: 'hidden', padding: '2px', isolation: 'isolate'}} title="Link disabled in preview mode">
<div style={{position: 'absolute', inset: '0', width: '400%', height: '100%', background: 'linear-gradient(115deg,#8b5cf6,#4c1d95,#a855f7)', backgroundSize: '25% 100%', animation: 'border-shift .75s linear infinite'}}></div>
<span className="inline-flex items-center hover:text-white hover:bg-white/10 hover:border-white/30 transition-all duration-300 group text-base font-light text-slate-300 font-geist bg-white/5 border-white/20 border rounded-xl pt-3 pr-6 pb-3 pl-6" id="aura-emeivefys" style={{position: 'relative', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: '0.5rem', padding: '1rem 1.5rem', fontSize: '1.1rem', background: '#4c1d95', borderRadius: '100px', height: '100%'}}>
<svg fill="none" height="20" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" viewbox="0 0 24 24" width="20" xmlns="http://www.w3.org/2000/svg">
<path d="M7 7h10v10"></path>
<path d="M7 17 17 7"></path>
</svg>
        Start Free Trial
      </span>
</a>
</div>
<a className="inline-flex items-center hover:bg-white/10 hover:border-white/30 transition-all duration-300 group text-lg font-light text-slate-300 font-geist bg-white/5 border-white/20 border rounded-full px-8 py-4" href="#" id="aura-emeivee05" style={{}} title="Link disabled in preview mode">
<svg className="mr-2 h-4 w-4" fill="none" height="24" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="1.5" viewbox="0 0 24 24" width="24" xmlns="http://www.w3.org/2000/svg">
<polygon points="5,3 19,12 5,21"></polygon>
</svg>
  Watch Demo
</a>
</div>

</div>

<div className="relative mt-20 lg:mt-28">

<div className="pointer-events-none absolute -inset-10 opacity-30">
<div className="absolute -top-20 -left-20 h-40 w-40 rounded-full bg-fuchsia-500/20 blur-2xl animate-pulse duration-[3000ms]"></div>
<div className="absolute -bottom-16 -right-16 h-32 w-32 rounded-full bg-indigo-500/20 blur-2xl animate-pulse duration-[4000ms] delay-1000"></div>
</div>

<div className="relative mx-auto max-w-5xl">

<section className="relative overflow-hidden shadow-[0_0_0_1px_rgba(255,255,255,0.04),0_40px_80px_-20px_rgba(0,0,0,0.7)] opacity-0 animate-[fadeInUp_1000ms_ease-out_1800ms_forwards] bg-[#0f0f17]/90 border-white/10 border rounded-3xl translate-y-12 backdrop-blur-sm">

<div className="pointer-events-none absolute inset-0 bg-[radial-gradient(1400px_500px_at_50%_-30%,rgba(120,119,198,0.15),transparent_70%)]"></div>
<div className="relative grid gap-12 p-6 sm:p-8">

<div className="flex items-start justify-between gap-4 opacity-0 translate-y-6 animate-[fadeInUp_600ms_ease-out_2200ms_forwards]">
<div className="flex items-start gap-3">
<div className="hidden sm:flex h-9 w-9 items-center justify-center rounded-xl bg-white/5 ring-1 ring-white/10 opacity-0 scale-95 animate-[fadeInScale_500ms_ease-out_2400ms_forwards] hover:bg-white/10 hover:ring-white/20 transition-all duration-200">
<svg className="lucide lucide-bar-chart-3 h-5 w-5 text-slate-200/80" data-lucide="bar-chart-3" fill="none" height="24" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="1.5" viewbox="0 0 24 24" width="24" xmlns="http://www.w3.org/2000/svg"><path d="M3 3v16a2 2 0 0 0 2 2h16"></path><path d="M18 17V9"></path><path d="M13 17V5"></path><path d="M8 17v-3"></path></svg>
</div>
<div className="">
<div className="text-sm text-slate-400/90 opacity-0 translate-x-4 animate-[fadeInLeft_500ms_ease-out_2500ms_forwards] font-geist font-light" style={{}}>Cashflow</div>
<div className="mt-1 text-4xl sm:text-5xl tracking-tight text-white opacity-0 translate-x-6 animate-[fadeInLeft_600ms_ease-out_2600ms_forwards] font-geist" id="cashflow-amount" style={{}}>$35,843.00</div>
</div>
</div>

<div className="flex items-center gap-2 opacity-0 translate-y-4 animate-[fadeInUp_500ms_ease-out_2700ms_forwards]">

<div className="flex items-center rounded-full border border-white/10 bg-white/5 p-1 hover:border-white/20 transition-colors duration-200">
<button className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3.5 py-1.5 text-sm text-white shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] ring-1 ring-white/10 transition-all duration-200 hover:bg-white/15 font-geist font-light" id="monthly-btn" style={{}}>
<span className="h-1.5 w-1.5 rounded-full bg-white"></span>
                          Monthly
                        </button>
<button className="inline-flex items-center rounded-full px-3.5 py-1.5 text-sm text-slate-400 hover:text-slate-200 hover:bg-white/5 transition-all duration-200 font-geist font-light" id="yearly-btn" style={{}}>
                          Yearly
                        </button>
</div>

<div className="relative">
<button className="hidden sm:inline-flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-sm font-medium text-slate-300 hover:text-white hover:border-white/20 hover:bg-white/10 transition-all duration-200" id="date-btn">
<svg className="lucide lucide-calendar h-4 w-4" data-lucide="calendar" fill="none" height="24" stroke="currentColor" strokeLinecap="round" strokeLinejoin="round" strokeWidth="1.5" viewbox="0 0 24 24" width="24" xmlns="http://www.w3.org/2000/svg"><path d="M8 2v4"></path><path d="M16 2v4"></path><rect height="18" rx="2" width="18" x="3" y="4"></rect><path d="M3 10h18"></path></svg>
<span className="font-geist font-light" id="date-range" style={{}}>Oct 12–20</span>
</button>
</div>
</div>
</div>

<div className="relative opacity-0 translate-y-8 animate-[fadeInUp_700ms_ease-out_2800ms_forwards]">

<div className="pointer-events-none absolute inset-0 -z-0 rounded-2xl">
<div className="absolute inset-0 rounded-2xl ring-1 ring-white/5"></div>
<div className="absolute inset-px rounded-[calc(theme(borderRadius.2xl)-1px)] bg-gradient-to-b from-white/[0.04] to-white/[0.02]"></div>
</div>

<div className="relative z-10 h-80 sm:h-96 px-1 pt-3">
<div className="relative h-full">
<canvas className="!h-full !w-full opacity-0 animate-[fadeIn_600ms_ease-out_3000ms_forwards]" height="744" id="cashflowChart" style={{display: 'block', boxSizing: 'border-box', height: '372px', width: '950px', cursor: 'default'}} width="1900"></canvas>

<div className="pointer-events-none absolute hidden min-w-[220px] rounded-2xl border border-white/10 bg-[#11111a]/90 p-4 text-sm text-slate-200 shadow-xl backdrop-blur transition-all duration-200" id="chart-tooltip" style={{display: 'block', opacity: '1', left: '512.479px', top: '97.5625px'}}>
<div className="mb-2 flex items-center justify-between">
<span className="text-slate-300 font-geist font-light" id="tooltip-date" style={{}}>Oct 16, 2023</span>
<span className="rounded-full bg-white/5 px-2 py-0.5 text-[10px] text-slate-300 ring-1 ring-white/10 font-geist font-light" style={{}}>Insight</span>
</div>
<div className="grid grid-cols-2 gap-y-1 text-[13px]">
<span className="text-slate-400 font-geist font-light" style={{}}>Cashflow</span>
<span className="text-right text-white font-geist font-light" id="tooltip-cashflow" style={{}}>$15,000.00</span>
<span className="text-slate-400 font-geist font-light" style={{}}>Inflow</span>
<span className="text-right text-fuchsia-300 font-geist font-light" id="tooltip-inflow" style={{}}>-$6,750.00</span>
</div>
</div>
</div>
</div>
</div>
</div>
</section>
</div>
</div>
</div>
</div>
</main>

<div className="fixed w-80 overflow-hidden shadow-[0_0_0_1px_rgba(255,255,255,0.04),0_30px_60px_-15px_rgba(0,0,0,0.6)] bg-[#0f0f17]/90 border-white/10 border rounded-3xl opacity-0 scale-95 translate-y-2 pointer-events-none transition-all duration-200 z-[9999] backdrop-blur-xl" id="calendar-dropdown">

<div className="pointer-events-none absolute inset-0 bg-[radial-gradient(800px_300px_at_50%_-10%,rgba(120,119,198,0.08),transparent_60%)]"></div>
<div className="relative p-6">

<div className="mb-5">
<div className="text-xs text-slate-400/90 mb-3 tracking-tight font-geist font-light" style={{}}>Quick Select</div>
<div className="grid grid-cols-2 gap-2.5">
<button className="preset-btn rounded-xl border border-white/10 bg-white/5 px-3.5 py-2.5 text-sm text-slate-300 hover:text-white hover:bg-white/10 hover:border-white/20 transition-all duration-200 hover:shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] font-geist font-light" data-preset="last7" style={{}}>Last 7 Days</button>
<button className="preset-btn rounded-xl border border-white/10 bg-white/5 px-3.5 py-2.5 text-sm text-slate-300 hover:text-white hover:bg-white/10 hover:border-white/20 transition-all duration-200 hover:shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] font-geist font-light" data-preset="last30" style={{}}>Last 30 Days</button>
<button className="preset-btn rounded-xl border border-white/10 bg-white/5 px-3.5 py-2.5 text-sm text-slate-300 hover:text-white hover:bg-white/10 hover:border-white/20 transition-all duration-200 hover:shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] font-geist font-light" data-preset="thisMonth" style={{}}>This Month</button>
<button className="preset-btn rounded-xl border border-white/10 bg-white/5 px-3.5 py-2.5 text-sm text-slate-300 hover:text-white hover:bg-white/10 hover:border-white/20 transition-all duration-200 hover:shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] font-geist font-light" data-preset="lastMonth" style={{}}>Last Month</button>
</div>
</div>

<div className="relative h-px bg-white/10 mb-5">
<div className="absolute inset-0 bg-gradient-to-r from-transparent via-white/20 to-transparent"></div>
</div>

<div className="space-y-4">
<div className="text-xs text-slate-400/90 tracking-tight font-geist font-light" style={{}}>Custom Range</div>
<div className="grid grid-cols-2 gap-3">
<div className="space-y-2">
<label className="text-xs text-slate-400 font-geist font-light" style={{}}>From</label>
<input className="w-full rounded-xl border border-white/10 bg-white/5 px-3.5 py-2.5 text-sm text-slate-200 font-geist placeholder:text-slate-500 focus:border-white/30 focus:bg-white/10 focus:outline-none focus:ring-1 focus:ring-white/20 transition-all duration-200" id="start-date" type="date"/>
</div>
<div className="space-y-2">
<label className="text-xs text-slate-400 font-geist font-light" style={{}}>To</label>
<input className="w-full rounded-xl border border-white/10 bg-white/5 px-3.5 py-2.5 text-sm text-slate-200 font-geist placeholder:text-slate-500 focus:border-white/30 focus:bg-white/10 focus:outline-none focus:ring-1 focus:ring-white/20 transition-all duration-200" id="end-date" type="date"/>
</div>
</div>
<div className="flex gap-3 pt-3">
<button className="flex-1 rounded-xl bg-white/10 px-4 py-2.5 text-sm text-white hover:bg-white/15 transition-all duration-200 ring-1 ring-white/10 hover:ring-white/20 shadow-[inset_0_1px_0_0_rgba(255,255,255,0.06)] font-geist font-light" id="apply-range" style={{}}>Apply Range</button>
<button className="px-4 py-2.5 text-sm text-slate-400 hover:text-white hover:bg-white/5 rounded-xl transition-all duration-200 font-geist font-light" id="cancel-range" style={{}}>Cancel</button>
</div>
</div>
</div>
</div>
<style>
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(2rem);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes fadeInDown {
        from {
          opacity: 0;
          transform: translateY(-1rem);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes fadeInLeft {
        from {
          opacity: 0;
          transform: translateX(1rem);
        }
        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      @keyframes fadeInScale {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      @keyframes fadeIn {
        from {
          opacity: 0;
        }
        to {
          opacity: 1;
        }
      }

      /* Custom date input styling for dark theme */
      input[type="date"] {
        color-scheme: dark;
      }
      
      input[type="date"]::-webkit-calendar-picker-indicator {
        filter: invert(0.8) brightness(1.2);
        opacity: 0.7;
        cursor: pointer;
        transition: opacity 0.2s ease;
      }
      
      input[type="date"]::-webkit-calendar-picker-indicator:hover {
        opacity: 1;
      }

      input[type="date"]::-webkit-datetime-edit {
        color: rgb(226 232 240);
      }
      
      input[type="date"]::-webkit-datetime-edit-text {
        color: rgb(148 163 184);
      }
      
      input[type="date"]::-webkit-datetime-edit-month-field {
        color: rgb(226 232 240);
      }
      
      input[type="date"]::-webkit-datetime-edit-day-field {
        color: rgb(226 232 240);
      }
      
      input[type="date"]::-webkit-datetime-edit-year-field {
        color: rgb(226 232 240);
      }

      input[type="date"]::-webkit-calendar-picker-indicator {
        background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 20 20' fill='rgba(226, 232, 240, 0.8)'%3e%3cpath fill-rule='evenodd' d='M6 2a1 1 0 00-1 1v1H4a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V6a2 2 0 00-2-2h-1V3a1 1 0 10-2 0v1H7V3a1 1 0 00-1-1zm0 5a1 1 0 000 2h8a1 1 0 100-2H6z' clip-rule='evenodd'/%3e%3c/svg%3e");
      }

      /* Smooth scrolling */
      html {
        scroll-behavior: smooth;
      }
    </style>


    </>
  );
}

# Interactive Background Gradient Builder

UI to customize animated background gradients with color, shape, speed, rotation, and style controls in real time.

## Project Details
- **Slug:** `4510IMQ`
- **Views:** 119
- **Tags:** Background Builder, Gradient, Animation, Shader, Tailwind CSS, Color Picker, Interactive UI
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

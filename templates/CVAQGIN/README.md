# Workflow Integration Landing Page

Responsive landing page with header, hero section, and email CTA using Tailwind CSS and SVG icons for workflow integration promotion.

## Project Details
- **Slug:** `CVAQGIN`
- **Views:** 854
- **Tags:** Landing Page, Hero Section, Tailwind CSS, Responsive Design, SVG Icons, Email CTA
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

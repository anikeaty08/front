# Thermostat Control UI in iPhone Frame

Responsive thermostat interface with animated iPhone mockup using Tailwind CSS and custom slider styling.

## Project Details
- **Slug:** `HTKAT57`
- **Views:** 183
- **Tags:** Thermostat, iPhone Frame, Tailwind CSS, Animation, Slider, Responsive UI
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

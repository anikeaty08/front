# User Sign-In Interface with Effects

Responsive sign-in page with animated floating backgrounds, gradient buttons, custom inputs, and smooth fade-in feature animations.

## Project Details
- **Slug:** `IACEJFI`
- **Views:** 999
- **Tags:** Sign-In, Animation, Floating Background, Gradient Buttons, Custom Inputs, Tailwind CSS, Responsive UI
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

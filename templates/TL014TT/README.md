# Dynamic Flower Background Canvas

Renders an animated blue flower pattern using WebGL shaders on a fullscreen canvas background.

## Project Details
- **Slug:** `TL014TT`
- **Views:** 80
- **Tags:** WebGL, Shader, Canvas, Animated Background, Flower, GLSL, Visual Effects
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

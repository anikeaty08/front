# Flight Search Filter UI

HTML and CSS code for a flight search filter interface with price sliders, sorting, stops, travel time, baggage options, and reset functionality.

## Project Details
- **Slug:** `VEOdtdkV`
- **Views:** 96
- **Tags:** HTML, CSS, UI, Flight Filter, Range Slider, Custom Controls, Responsive Design
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

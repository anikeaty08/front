# CSS Parent Selector Usage UI

Displays a styled tip explaining the CSS :has() pseudo-class to target parent elements based on child selectors.

## Project Details
- **Slug:** `X108OAJ`
- **Views:** 46
- **Tags:** CSS Tip, Parent Selector, :has(), Tailwind CSS, Code Example, UI Card
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

# App Icon Grid with Background

Displays a grid of app icons with labels on a styled background using Tailwind CSS and SVG icons.

## Project Details
- **Slug:** `XAFY5JQ`
- **Views:** 215
- **Tags:** App Grid, Icons, Tailwind CSS, SVG, Background Overlay, UI Layout
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

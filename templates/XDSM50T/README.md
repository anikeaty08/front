# Animated Mesh Gradient Triangle UI

Creates a dark mode UI with an animated triangle and layered mesh gradient blobs using CSS animations and Tailwind.

## Project Details
- **Slug:** `XDSM50T`
- **Views:** 431
- **Tags:** Mesh Gradient, CSS Animation, Dark Mode, Tailwind CSS, SVG Clip-path, Responsive Design
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

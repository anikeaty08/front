# Email Composition Modal UI

Responsive modal interface for composing and sending emails with recipient, subject, and interactive UI elements.

## Project Details
- **Slug:** `Y48HUHS`
- **Views:** 531
- **Tags:** Modal, Email Compose, Tailwind CSS, Responsive UI, Form, Popover
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

# Feature Grid with 3D Background

Displays a responsive grid of interactive feature cards over a full-screen 3D animated background using Three.js and Tailwind CSS.

## Project Details
- **Slug:** `guywyyZ`
- **Views:** 108
- **Tags:** Features, 3D Background, Tailwind CSS, Three.js, Animation, Responsive Grid, UI Cards
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

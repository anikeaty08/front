# Semantic SQL Agent Dashboard Template

A modern UI template for a Semantic SQL Agent dashboard, featuring CSV import, schema inspection, natural language to SQL generation, and activity logging.

## Project Details
- **Slug:** `semantic-sql-41`
- **Views:** 15
- **Tags:** Technology, Landing Page, Brand, Clean, Minimal, Dashboard, SQL, Data Tool, AI Agent
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.

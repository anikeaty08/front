# Recording Mode Selector UI Template

A reusable recording mode selector UI template for choosing screen, camera, or combined recording with position and shape controls for modern web apps.

## Project Details
- **Slug:** `talki-recording-modes`
- **Views:** 7
- **Tags:** Technology, Landing Page, Brand, Clean, Minimal, Gradient, Large Type, UI component, recording widget, video tool
- **Template Type:** Vite-React Project (Compiled from static HTML)

## How to Run
1. Open folder in VS Code / terminal.
2. Run `npm install`.
3. Run `npm run dev`.
